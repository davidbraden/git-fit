# AI-001 — Add AI ticket workflow

## Asked
- Set up agent instructions and an SDLC workflow similar to the nearby
  `habeeri` project.

## Decisions
- Mirror `habeeri`'s lightweight, repository-local AI session ticket process.
- Use `.ai/tickets/` with sequential `AI-###` Markdown records.

## Changed
- Added `AGENTS.md` to require a concise AI ticket for each completed session
  or commit.
- Added the workflow guidance and template in `.ai/tickets/README.md`.
- Added this initial setup record.
